# Scan 149 — user lexical-control reconciliation

Date: **2026-08-26**

## Why scan 149 was reopened

The earlier recovery transcription had repeatedly interpreted older Tamil letterforms as `-உன்` forms (`என்றுன்`, `நின்றுன்`, `சென்றுன்`). The user supplied a page-149 lexical extraction after the same failure pattern had already been demonstrated on scans 146–148.

The supplied extraction is therefore the lexical control for this reconciliation. The physical scan remains useful for page identity and directly unambiguous structure such as visible separator count.

## Superseded readings

| Previous repository reading | User lexical control |
|---|---|
| `சுக்கிரிவன்` | `சுக்ரீவன்` |
| `கடல்அலைமேல்` | `கடல் அலைமேல்` |
| `விபீஷணு!` | `விபீஷணா !` |
| `என்றுன்!` | `என்றான்!` |
| `நின்றுன்—` | `நின்றான்-` |
| `தென்னவனும்-கண்கள்` | `தென்னவனும் - கண்கள்` |
| `கோடரியின் பிடியே!.....கொடிய` | `கோடரியின் பிடியே!.... கொடிய` |
| `அறுத் தெறியாமல்` | `அறுத்தெறியாமல்` |
| `களம் நோக்கி...!` | `களம் நோக்கி!` |
| `உத்தமனும்—சிந்தன்` | `உத்தமனாம் - சிந்தனை` |
| `வல்லவனும்` | `வல்லவனாம்` |
| `உடை வாளிப் போலப்` | `உடை வாளைப் போலப்` |
| `நின்றுன்,` | `நின்றான்,` |
| `ஜடை கட்டி` | `ஐடை கட்டி` |
| `பயனில்லை-பாழாகும் என்றுன்!` | `பயனில்லை- பாழாகும் என்றான்!` |
| `நடை கட்டு` | `நடைகட்டு` |
| `சென்றுன்,` | `சென்றான்,` |
| `இன்றிகத்` | `இன்றிந்தத்` |

## Structure retained from the page image

The physical page visibly contains **three `★` separators**. The supplied pasted extraction represented the first as `*` and omitted the third separator before the final speech, so the visible three-separator structure is retained in `pages/0149.md`.

The supplied final line repeated `ஆண்டிருக்கும்` twice consecutively. The page visibly contains one occurrence; the canonical page therefore retains one and records that resolution explicitly rather than silently duplicating it.

## Result

The previous scan-149 recovery PASS is **superseded**. `pages/0149.md` now uses the user-supplied lexical reading and records the repeated old-glyph confusion that had caused `-ஆன்` to be misread as `-உன்`.