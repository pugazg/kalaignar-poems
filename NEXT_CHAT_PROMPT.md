# Next Chat Prompt — Kalaignar Poems Archive / கலைஞரின் கவியரங்கக் கவிதைகள் (1975)

Continue directly in `pugazg/kalaignar-poems`, branch `main`, active workspace `poems/kalaignarin-kaviyaranga-kavithaigal-1975/`.

## LIVE MAIN IS AUTHORITATIVE

Fetch live `main` first. Preserve newer durable work and do not reopen release-cleared works because a copied prompt is older.

Before changes read `POEM_PROCESSING_GUIDE.md`, root `TRANSCRIPTION_PHASE_PLAN.md`, root `HANDOVER.md`, this prompt, then active-work `README.md`, `SOURCE_INTAKE.md`, `metadata/source.md`, `indexes/item-map.md`, `indexes/page-map.md`, `audit.md`, `HISTORICAL_GLYPH_AUDIT.md`, `HANDOVER.md`, and `NEXT_CHAT_PROMPT.md`.

Controlling source: `TVA_BOK_0064169_கலைஞரின்_கவியரங்கக்_கவிதைகள்.pdf` — **84 scans**, **93,307,011 bytes**, SHA-256 **PENDING**. Resolve the same supplied attachment before visual work; never substitute another edition.

New Kalaignar blocks: scans **46–57, 58–65, 66, 67–68**. Existing cross-witness blocks: **9–20, 21–32, 33–45, 71–77, 78–84**. Bharathidasan insert: **69–70**.

## Historical Tamil glyph control

Use the user-supplied `HISTORICAL_TAMIL_GLYPH_TRANSCRIPTION_GUIDE.md` as a required verification aid. On every Tamil text page screen:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

Read character identity from source pixels first and encode that identity in normal Unicode. Do not modernize spelling, grammar, vocabulary, punctuation or lineation; never global-replace. This first pass does not satisfy Phase 2 verification.

## Durable checkpoint

Phase 1 page records: **12/84**.

- `partial`: scans **1, 2, 6, 8–12**;
- `needs-review`: scans **3, 4, 5, 7**;
- historical-glyph first-pass: scans **9–12 complete**;
- verified: **0**;
- next physical scan: **13**.

Scans 9–12 are the 1975 Anna-poem cross-witness. Representative checked identities include `அண்ணா` (`ணா`), `அன்னை` (`னை`), `நினைவுண்டா` (`னை`) and `மாற்றான்` (`றா`). The release-cleared Anna poem remains unchanged.

Unresolved source text remains deliberately open on scan 3 (small imprint), scan 4 (cursive handwriting), scan 5 (`முன்னுரை` body), and scan 7 (`பதிப்புரை` body/role lines). Do not guess those readings.

The exact supplied attachment remains controlling and its size matches the intake record. Exact SHA-256 computation is still unavailable through the current execution paths. Keep SHA-256 visibly pending and reattempt when possible; do not invent it.

## Exact next activity

Continue **Phase 1 transcription only from scan 13 through scan 20 in physical order**, one page record per scan. Complete the already represented Anna-poem cross-witness, preserve the 1975 source spelling, punctuation, lineation and printed page numerals exactly, and apply the historical-glyph screen to each page. Do not reopen or modify the existing release-cleared Anna canonical work merely because this edition differs.

Do not begin Phase 2 verification, Phase 3 canonical assembly or Phase 4 translation.

Preserve `poems/gunanayagar-nehru/`, `poems/oruthalaik-kathal/` and `poems/kalaignarin-kavithaigal/` as RELEASE-CLEARED.
